
# This is your Prisma database connection string.
# Replace with your actual connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Set to true to use mock data in Firebase Studio or other environments
# where direct database access might be problematic due to missing system libraries.
# For local development, keep this false or remove it to use the real database.
USE_MOCK_DATA=false
